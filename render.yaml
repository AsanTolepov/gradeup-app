services:
  - type: static site # "static_site" emas, "static site" (probel bilan)
    name: gradeup-app
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html